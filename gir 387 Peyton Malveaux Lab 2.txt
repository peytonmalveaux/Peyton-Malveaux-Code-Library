#include <stdio.h>
#include <stdlib.h>


	int*p = (int*)malloc(100* sizeof(int));

	if(p == null){
		printf("P is NULL");
		return 0;
	}	
	/* Check malloc for NULL to see is working properly. 
    
	If malloc is null then an error message will appear
   */

	for(i=0; i < 100; i++){
	p[i] = i;
	printf("%d", p[i]);

	} 

	for(int i = 0; i < 100; i++){
		p[i] = p[i]++;
	}

	for(int i = 0; i < 100; i++){
		printf("%d", sp[i]->angle);
	}
		
	

